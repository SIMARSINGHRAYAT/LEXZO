# Backend API URL (used by the frontend)
# For local development with Flask running on port 5000:
VITE_API_URL=http://localhost:5000
# For Docker deployment (nginx proxies /api → backend):
# VITE_API_URL=/api
